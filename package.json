{
  "name": "medcomply-monorepo",
  "private": true,
  "description": "Vertical medical compliance SaaS — frontend (Next.js) + backend (FastAPI) + Supabase",
  "scripts": {
    "dev:fe": "npm run dev --prefix frontend",
    "dev:be": "cd backend && py -m uvicorn api.main:app --reload --host 127.0.0.1 --port 8000",
    "build:fe": "npm run build --prefix frontend"
  }
}
